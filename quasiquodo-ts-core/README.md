# quasiquodo-ts-core

This crate is an internal implementation detail of [Quasiquodo](https://crates.io/crates/quasiquodo). It contains the core logic for preprocessing and parsing TypeScript source literals, and generating Rust code to construct the equivalent AST nodes.

**Please use `quasiquodo` directly.** This crate's API is not stable, and may change without notice.
